{
  "name": "ember-grain-platform",
  "version": "2.0.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter @ember-grain/db db:generate && pnpm --filter web build",
    "postinstall": "pnpm --filter @ember-grain/db db:generate",
    "start": "pnpm --filter web start",
    "lint": "pnpm -r --filter=./apps/* lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test",
    "db:generate": "pnpm --filter db prisma generate",
    "db:migrate": "pnpm --filter db prisma migrate dev",
    "db:migrate:deploy": "pnpm --filter packages/db prisma migrate deploy",
    "db:seed": "pnpm --filter db prisma db seed",
    "db:studio": "pnpm --filter db prisma studio"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@google/genai",
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "protobufjs"
    ]
  }
}
