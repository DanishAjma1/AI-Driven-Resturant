# Postgres, matches docker-compose.yml at the repo root.
DATABASE_URL="postgresql://{username}:{password}@localhost:5432/{database}?schema=public"
DIRECT_URL="add_direct_url_if_using_neon_or_supabase_etc"